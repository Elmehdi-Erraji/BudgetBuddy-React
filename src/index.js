import React from 'react';
import ReactDOM from 'react-dom/client';

import App from './App';

//here where all the components are rendered
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);


